spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://localhost:3306/eventdb
    username: root
    password: "root123"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081